logging:
  level:
    root: INFO

spring:
  application:
    name: feign-la
  cloud:
    zookeeper:
      discovery:
        enabled: true
        prefer-ip-address: true
      connect-string: localhost:2181

server:
  port: 9090